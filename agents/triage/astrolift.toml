# astrolift.toml — one agent, per the sanctioned agents/<slug>/ layout.
#
# Scans open findings and classifies them by severity.
#
# kind=agent + run_family=task means this spawns as a Job on dispatch and
# holds no resource between runs. Idle cost is zero.

astrolift_version = 1
name = "conflict-agent-triage"

[app]
slug = "conflict-agent-triage"
display_name = "Agent: triage"

[environments.production]

[[workloads]]
name = "triage"
kind = "agent"
run_family = "task"
cpu_request = "50m"
cpu_limit = "250m"
memory_request = "128Mi"
memory_limit = "256Mi"

  [[workloads.containers]]
  name = "triage"
  is_primary = true
  command = ["python", "agent.py"]

    [workloads.containers.env]
    PYTHONUNBUFFERED = "1"
    AGENT_ROLE = "triage"
